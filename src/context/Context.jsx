import { createContext } from "react";

export const Products = createContext([]);
export const Carts = createContext([]);
export const Users = createContext([]);
export const Posts = createContext([]);
export const Comments = createContext([]);
export const Todos = createContext([]);
export const Qoutes = createContext([]);
